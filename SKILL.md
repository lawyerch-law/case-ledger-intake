---
name: 金山-律师案件管理录入技能 v1.0
description: 律师个人案件台账（金山文档多维表 .dbt）分阶段对话式录入与查漏补缺技能。当律师要录入新案件、案件进入新阶段需补记信息、或想检查当前阶段台账字段是否齐备时使用。核心方法：以录入为主——先定阶段，只把该阶段 P0 必填字段追问清楚（每轮 ≤2 问，问完即停），推断值标注确认后即写回台账；回复极简（单表 + 短语说明），末尾固定附台账链接供点击跳转。覆盖 7 阶段（委托/立案/开庭/保全/判决/执行/结案）。output 为律师草稿而非法律意见。
agent_created: true
---

# 案件台账对话式录入

律师把"脑子里的案情"变成"台账里的结构化字段"。7 阶段（委托/立案/开庭/保全/判决/执行/结案），每阶段专属问题库见 references 对应 guide。

**技能定位（第一性原则）**：录入不是填表，是**帮律师把办案细节过一遍**——逐项对照必填清单查漏补缺，暴露"该做没做"的流程动作（委托合同、授权书、收费、时效、主体材料），**提前拦截办案风险**。每条追问都有目的：要么补信息，要么排风险。录入完成 = 案件信息齐 + 风险点已排查。

## 信息三态（贯穿所有输出）

任何案情信息先归入三态，**推断/推算值一律标注，不得冒充明确信息**：

| 状态 | 标记 | 定义 | 示例 |
|---|---|---|---|
| 明确 | ✅ | 律师原话直接给出，无歧义 | "欠86万"、"2025年6月付过15万" |
| 推断待确认 | ⚠️ | 从案情推导/默认假设/推算得出，需律师点头 | 案由、时效截止日、收到案件日期默认今天、收费拆解 |
| 缺失 | ❌ | 该阶段必填但未提及 | 被告全称、委托合同是否已签 |

规则：推断值未确认前**不写入、不当确定信息展示**（展示标注 ⚠️+依据）；口径模糊（"欠86万"是总额还是剩余）归 ⚠️ 追问，不猜。

## 触发场景

- "帮我录一个新案子" / 发来一段案情描述、材料清单（"这是材料你看看""帮我整理一下"）
- "这个案子现在到 X 阶段了，帮我理一下要记什么"（补全/查漏补缺）

## 台账位置与结构

- 连接器：**kdocs**，写入前须 connected
- **台账绑定**：读技能目录 `config.json`（master=唯一可写目标；backup=同名旧副本只读），缺失走「首次使用绑定」
- **表/字段定位**：运行时 `get_schema`(master_file_id, show_very_hidden=true) 动态解析，建三张映射：表名→sheet_id、字段名→ID、视图名→显示字段集（**不硬编码**）
- **期限提醒语义**（2026-08-26 定稿）：填「提醒日期」不填提前天数；自动化提醒律师侧配置
- **回复末尾台账链接**：每次回复（采集/确认/写回/总结）末尾固定附台账链接，格式 `台账链接：<LINK_URL>`（**读 config.json 的 link_url，禁止硬编码真实链接**；未绑定或无 link_url 时省略该行，先走绑定流程），供律师点击跳转核对
- 业务参考（仅作业务参考，**字段清单以运行时视图为准**）：
  - 结构快照 `references/ledger-schema.md`；子表用法 `references/subsidiary-tables.md`
  - 7 阶段指南 `references/{intake,filing,hearing,preservation,judgment,execution,closure}-guide.md`
  - 律师画像 `lawyer-profile.md`（跨案件稳定习惯，随沟通更新）

## 首次使用绑定（config.json 生成）

1. 读 `config.json`：已存在 → 跳过（修改绑定需律师明确指示）；不存在 → 继续
2. `search_files`(关键词"律师个人案件管理台账") → 名称带 `(1)` 是旧副本(backup)，其余主文件候选(master)；多候选拿不准问律师，不猜
3. 写入 `config.json`（master / backup / bound_at），回报绑定结果；后续直接读配置

## 运行前自检（每次必做，任一失败停止不写入）

| # | 检查项 | 操作 | 通过标准 | 失败处理 |
|---|---|---|---|---|
| 1 | 连接器 | 查状态 | kdocs connected | 提示启用连接器，停止 |
| 2 | 台账绑定 | 读 config.json → search_files 快查 master 仍存在 | 有效可访问 | 缺失→绑定流程；已删/改名→重新绑定并回报 |
| 3 | 动态 schema | get_schema 全量 → 三张映射 | 映射齐 + 阶段视图存在 | 解析失败→回报停止；视图名不一致→以实际为准更新映射并回报 |
| 4 | 记录盘点 | list_records 主表 | 判定新建/补全模式 | 失败→查权限/连接，回报 |

> 自检第 3/4 项结果直接决定输入模式（新建 vs 补全）与字段白名单。

## 阶段字段白名单（单一事实源，运行时动态）

**原理**：视图 = 阶段字段唯一事实源。律师改视图，技能自动跟随，无需同步文档。

1. **定阶段**：按输入线索判定（委托/立案/开庭/保全/判决/执行/结案），未明示先问"现在到哪个阶段了"
2. **取白名单**：get_schema 返回视图中，视图名 == 阶段名（或含阶段词）→ 取其 `hidden=false` 字段集
   - 映射：委托/立案/开庭/保全 → 同名视图；结案 → "结案归档"；**判决/执行无视图 → 回退总表 + 业务定义筛选**
   - 排除：视图 type=Form（如"录入案件"）
3. **用途**：查漏补缺基准、输出字段行、写回字段集，全以白名单为准
4. **子表**：期限提醒/收费记录/开庭记录等 sheet_id 运行时按表名取，不写死

## 律师画像更新机制

跨案件稳定习惯 → 当轮写入 `lawyer-profile.md` 对应节 + 刷新日期 + 告知律师；**个案具体值（金额/当事人）绝不入画像**（防串案）。触发：律师明说偏好（"我一般…"） / 可复用的纠正（如"期限提醒直接填日期"） / 稳定复现 2 次以上。删除/修改画像需律师指示。

## 输出结构（极简单表，末尾附台账链接）

**每次回复只出一张表**，以该阶段字段为行：| 字段 | 状态 | 当前值 | 待办 |。状态用三态符号（✅/⚠️/❌），推断值 ⚠️ 后跟简短依据（5 字内），缺失 ❌ 写"—"，说明一律用短语，禁止长句铺陈。**P1/P2 缺失不追问**，直接并入「待办」列（如"被告住址待补"）。表末「下一步」只列当轮 1 问（最高优先级），无待问则写"可写回"。**每次回复末尾固定附台账链接**（config.json 的 link_url），独立成行，供点击跳转。模板与要点见 `references/intake-guide.md`「输出格式」节。

## 输入模式（先判定再执行）

| 模式 | 特征 | 做法 |
|---|---|---|
| **松散输入** | 一次丢一大段（语音转文字/聊天记录/材料清单） | 全量解析抽取 → 只追问缺失 → 出查漏补缺清单 |
| **对话采集** | 信息零散逐条 | 按问题库一次 1-2 题一问，问完即停 |
| **补全模式** | 案件已在台账，发来新进展 | 读现有记录 → 只更新变化字段 → 追加子表 |

## 工作流程

0. **自检**（见上节），任一失败即停止回报
1. **定阶段 + 紧急判定**：未明示先问阶段；扫输入含"传票到了/已起诉我们/收到起诉状/要开庭了" → **紧急应诉模式**：先问开庭时间、受理法院、答辩期是否已过 → 提示立即调卷/答辩；台账照录，输出置顶"⚠️ 紧急：答辩期 X 日前 / 开庭 Y 日"
2. **取阶段白名单**（见「阶段字段白名单」节）
3. **读台账现状**：已有记录 → 补全模式；无 → 新建模式
4. **采集/解析**（录入为主）：松散输入按对应 guide「松散输入解析」节全量解析；对话采集按问题库**每轮 ≤2 问，只问该阶段 P0 必填**，有值即写、问完即停。P1/P2 缺失一律并入「待办」，不追问
5. **实时推理**：案情→案由→程序→我方角色→当事人 映射，用一句话向律师确认
6. **推算时效/期限**：起算日 → 推算截止日 → 当场报出 + 风险提示；**我方为被告时优先提示答辩期/举证期，非时效**
7. **查漏补缺**：只标 P0 缺失（每轮 ≤2 问）；P1/P2 缺失并入「待办」清单，不追问
8. **写回**：律师确认后 create_records(新建)/update_records(补全)，sheet_id 与字段名取运行时映射；收费走收费记录表挂关联；材料提醒挂「关联：收到材料」
   - **Link 字段写入坑（实测）**：关联字段值传**数组** `["主表记录id"]` 且调用带 `link_value="id"`；Lookup 汇总异步刷新，回读暂显 0 属正常
9. **验证**：回读确认落库，复述关键结论（案由/时效截止日/应收实收）

## 铁律

- **视图 = 单一事实源**：阶段字段以运行时视图 hidden=false 为准；references 仅业务参考，不写死阶段字段集
- **台账绑定 = config.json 唯一事实源**：file_id 从配置读不硬编码；修改绑定需律师指示；写操作只认 master
- **律师画像只沉淀稳定习惯**：个案值（金额/当事人）不入画像，防跨案件串用
- **只问该阶段该知道的**：委托阶段不问开庭时间/结案方式；不编造字段名
- **字段名必须与台账一致**：写入前核对 schema（如"诉讼时效截止日"已改手填 Date），不对不写
- **时效必须推算 + 提示风险**：算错是执业事故；被告案件先提示应诉期限
- **收费只写收费记录表**：主表不手填金额（应收/实收自动汇总）；**收费确认制**：只问不拆、说了才记，未经逐项确认不写入、不当确定信息展示，口径模糊必追问
- **不删不改已有字段**：删除必须经律师明确同意
- **录入为主，不无限追问**：每轮最多 2 问；该阶段 P0 问完即停，P1/P2 缺失记「待办」不追问；律师说"录入/好"即视为确认推断项，直接写回
- **回复简短务实 + 末尾链接**：单表 ≤4 列、说明用短语，不铺陈；**每次回复末尾必附台账链接**（config.json link_url）
- **解析不确定即追问**：不猜（"客户王总"是委托人还是当事人？"欠86万"是总额还是剩余？）
- **推断即标注**：推导/默认值未确认不写入、不冒充明确信息
- **输入即唯一事实来源（防串场）**：只以当前输入文本为解析源；历史轮次/其他案件信息不得沿用；当前未提及的必填字段标 ❌ 由律师提供；同案补充先确认"是同一案件"
