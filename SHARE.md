# 技能分享指南（分发前必读）

本技能可分享给其他律师使用，但含**个人数据**，分发前必须清理以下文件，否则会泄露你的台账绑定、执业信息与真实案件痕迹。

## ⚠️ 分发前必须删除/清空（个人数据）

| 文件 | 含什么 | 处理 |
|---|---|---|
| `config.json` | 你的台账 file_id、跳转链接（私有绑定） | **整文件删除**。新用户首次运行会自动走「首次使用绑定」流程生成自己的 config.json |
| `lawyer-profile.md` | 律师画像：姓名、执业地、默认法院、收费习惯 | **整文件删除** 或保留结构清空值（见下节模板） |

## 文档内已清理项（无需再动）

以下真实数据已在本版本中替换为占位符，分发时**不要再改回来**：
- file_id（master/backup）→ 已移除，只写"以 config.json 为准"
- 台账跳转链接 → 已改为 `<LINK_URL>` 占位符
- 真实姓名 → 已改为"默认代理律师（见 lawyer-profile.md）"
- 本地法院列表 → 已改为"选项以实际 schema 为准"
- 真实案件示例（含真实案号）→ 已改为 X 先生 / 某某区人民法院 / XXXX 号占位

## lawyer-profile.md 空白模板（可选，清空后随包分发）

```markdown
# 律师画像（个人配置，随沟通更新）

## 基本信息
- 姓名：（你的姓名，台账「代理律师」默认值）
- 执业地：（省份/城市）

## 收费习惯
- 收费项目拆分：（如"前期代理费 / 风险代理费"）

## 常用机构
- 默认审理机关：（台账单选既有值）

## 办案偏好
- （其他稳定习惯，如"期限提醒直接填提醒日期"）
```

> 画像机制是技能的一部分（跨案件稳定习惯沉淀）；值是个人的。分享 = 空模板，自用 = 填真实值。

## 其他注意

- references 下各 guide 中示例均为通用化占位，不含任何真实当事人/案号/金额
- 若你后续在文档中新增示例，**一律用占位符**（X 先生 / 某法院 / XXXX 号），禁止写入真实案件数据
